# Pinned to the versions this repository was built, trained and tested against.
# Python 3.14.6 on Windows 11. CPU only - no CUDA, no cloud, no API keys.
# Model artifacts in models/ are pickled by scikit-learn; changing the
# scikit-learn pin below without re-running scripts/train.py will make
# src/models.py report a version mismatch and degrade to rules-only.

numpy==2.4.6
scipy==1.18.1
pandas==3.0.3
scikit-learn==1.9.0
joblib==1.5.3

fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5

pytest==9.1.1
httpx==0.28.1
